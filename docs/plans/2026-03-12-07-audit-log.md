# 审计日志系统 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 记录所有用户关键操作（交易增删改、设置变更、报告生成、AI 分类确认），为 CPA 合规和用户自查提供完整操作历史。

**Architecture:** 新增 `AuditLog` Prisma 模型。通过 Fastify hook 或显式调用记录操作。API 提供只读查询（支持过滤、分页）。前端在 Settings 中添加 "Activity Log" 查看页。

**Tech Stack:** Prisma, Fastify, Next.js

---

### Task 1: Prisma Schema — AuditLog 模型

**Files:**

- Modify: `apps/api/prisma/schema.prisma`

**Step 1: 添加 AuditLog 模型**

```prisma
model AuditLog {
  id         String   @id @default(uuid())
  userId     String
  user       User     @relation(fields: [userId], references: [id])
  action     AuditAction
  entityType String   // "transaction", "taxReport", "dataSource", "settings", etc.
  entityId   String?  // 被操作实体的 ID
  details    Json?    // 变更详情 { before: {...}, after: {...} }
  ipAddress  String?
  userAgent  String?
  createdAt  DateTime @default(now())

  @@index([userId, createdAt])
  @@index([userId, action])
  @@index([entityType, entityId])
}

enum AuditAction {
  CREATE
  UPDATE
  DELETE
  BULK_DELETE
  IMPORT
  EXPORT
  CALCULATE
  GENERATE_REPORT
  AI_CLASSIFY
  AI_CONFIRM
  AI_CORRECT
  LOGIN
  LOGOUT
  SETTINGS_CHANGE
  SUBSCRIPTION_CHANGE
}
```

**Step 2: Run migration**

Run: `cd apps/api && npx prisma migrate dev --name add-audit-log`

**Step 3: Commit**

```bash
git add apps/api/prisma/
git commit -m "feat(db): add AuditLog model for compliance tracking"
```

---

### Task 2: Audit Service

**Files:**

- Create: `apps/api/src/lib/audit.ts`
- Test: `apps/api/src/__tests__/audit.test.ts`

**Step 1: Write the failing test**

```typescript
describe("Audit Service", () => {
  it("should create an audit log entry", async () => {
    const entry = await logAudit({
      userId: "user-1",
      action: "CREATE",
      entityType: "transaction",
      entityId: "tx-123",
      details: { type: "BUY", asset: "BTC", amount: "0.5" },
      ipAddress: "127.0.0.1",
    });
    expect(entry.id).toBeDefined();
    expect(entry.action).toBe("CREATE");
  });

  it("should query logs with filters", async () => {
    const logs = await getAuditLogs("user-1", {
      action: "CREATE",
      entityType: "transaction",
      limit: 10,
    });
    expect(Array.isArray(logs.data)).toBe(true);
    expect(logs.total).toBeGreaterThanOrEqual(0);
  });
});
```

**Step 2: Run test — expect FAIL**

**Step 3: Implement**

```typescript
// apps/api/src/lib/audit.ts
import { prisma } from "./prisma.js";

interface AuditInput {
  userId: string;
  action: string;
  entityType: string;
  entityId?: string;
  details?: Record<string, unknown>;
  ipAddress?: string;
  userAgent?: string;
}

export async function logAudit(input: AuditInput) {
  return prisma.auditLog.create({ data: input });
}

interface AuditQuery {
  action?: string;
  entityType?: string;
  from?: Date;
  to?: Date;
  limit?: number;
  offset?: number;
}

export async function getAuditLogs(userId: string, query: AuditQuery = {}) {
  const where: Record<string, unknown> = { userId };
  if (query.action) where.action = query.action;
  if (query.entityType) where.entityType = query.entityType;
  if (query.from || query.to) {
    where.createdAt = {};
    if (query.from) (where.createdAt as Record<string, Date>).gte = query.from;
    if (query.to) (where.createdAt as Record<string, Date>).lte = query.to;
  }

  const [data, total] = await Promise.all([
    prisma.auditLog.findMany({
      where,
      orderBy: { createdAt: "desc" },
      take: query.limit || 50,
      skip: query.offset || 0,
    }),
    prisma.auditLog.count({ where }),
  ]);

  return { data, total };
}
```

**Step 4: Run test — expect PASS**

**Step 5: Commit**

```bash
git add apps/api/src/lib/audit.ts apps/api/src/__tests__/audit.test.ts
git commit -m "feat(api): add audit log service"
```

---

### Task 3: 在关键路由中添加审计日志

**Files:**

- Modify: `apps/api/src/routes/transactions.ts` (CREATE, UPDATE, DELETE, BULK_DELETE)
- Modify: `apps/api/src/routes/tax.ts` (CALCULATE, GENERATE_REPORT)
- Modify: `apps/api/src/routes/import.ts` (IMPORT)
- Modify: `apps/api/src/routes/ai-classify.ts` (AI_CLASSIFY, AI_CONFIRM, AI_CORRECT)
- Modify: `apps/api/src/routes/auth.ts` (LOGIN, LOGOUT)
- Modify: `apps/api/src/routes/billing.ts` (SUBSCRIPTION_CHANGE)

**Step 1: 在每个关键 handler 末尾添加 logAudit 调用**

示例（transactions.ts — create）:

```typescript
// 在成功创建交易后
await logAudit({
  userId: request.userId,
  action: "CREATE",
  entityType: "transaction",
  entityId: transaction.id,
  details: { type: body.type, asset: body.sentAsset || body.receivedAsset },
  ipAddress: request.ip,
  userAgent: request.headers["user-agent"],
});
```

**注意:** 审计日志调用使用 fire-and-forget（不阻塞主流程），用 `.catch()` 静默处理错误。

**Step 2: Commit**

```bash
git add apps/api/src/routes/
git commit -m "feat(api): add audit logging to all critical route handlers"
```

---

### Task 4: Audit Log API Route

**Files:**

- Create: `apps/api/src/routes/audit.ts`
- Modify: `apps/api/src/index.ts`

**Step 1: 实现端点**

- `GET /audit` — 查询自己的审计日志（支持 action、entityType、from、to 过滤 + 分页）
- `GET /admin/audit/:userId` — Admin 查询任意用户审计日志

**Step 2: Commit**

---

### Task 5: Web UI — Activity Log

**Files:**

- Modify: `apps/web/src/lib/api.ts`
- Modify: `apps/web/messages/en.json` / `zh.json`
- Modify: `apps/web/src/app/[locale]/settings/page.tsx`

**Step 1: API 函数 + i18n**

**Step 2: Settings 页面添加 "Activity Log" 折叠区**

显示最近 50 条操作记录：时间、操作类型、目标、详情。支持过滤。

**Step 3: Commit**

---

### Task 6: CPA 查看客户审计日志

**Files:**

- 依赖 Plan 01 (CPA Multi-Client)
- Modify: `apps/api/src/routes/audit.ts` (支持 `?clientId=`)

CPA 可查看自己管理的客户的审计日志，用于合规审查。

---

### Task 7: 五步法审计

**审计重点:**

- 审计日志不记录敏感数据（密码、API key 内容等）
- 仅记录操作元数据和变更概要
- fire-and-forget 不影响主流程性能
- Admin 端点有权限检查
