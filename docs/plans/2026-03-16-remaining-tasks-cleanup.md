# 剩余任务清理 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 完成审计中发现的 3 个未完成项：i18n 无障碍修复、form8949-pdf 大文件重构、CLI 测试扩展。

**Architecture:** 三个独立任务，无互相依赖。(1) Nav 组件 locale 按钮添加 aria-label，(2) 451 行 form8949-pdf.ts 拆分为 3 个子模块（render-form8949.ts + render-schedule-d.ts + pdf-utils.ts），(3) CLI 集成测试从 13 扩展到 30+。

**Tech Stack:** Next.js, next-intl, pdfkit, Vitest, tsx CLI

---

## Priority Order

| #   | Task                                    | Impact | Area       |
| --- | --------------------------------------- | ------ | ---------- |
| 1   | i18n locale 切换按钮 aria-label 修复    | Low    | Web (a11y) |
| 2   | form8949-pdf.ts 大文件重构 (451→3 文件) | Medium | Tax Engine |
| 3   | CLI 集成测试扩展 (13→30+)               | Medium | CLI        |

---

### Task 1: i18n Locale 切换按钮 aria-label 修复

**Files:**

- Modify: `apps/web/src/app/[locale]/nav.tsx`

**Step 1: 添加 aria-label 到 locale 切换按钮**

在 `apps/web/src/app/[locale]/nav.tsx` 中，locale 切换按钮（约第 252 行）当前代码：

```tsx
<button
  className="nav-link nav-more-trigger locale-switch"
  onClick={() => setLocaleOpen(!localeOpen)}
  aria-expanded={localeOpen}
  aria-haspopup="true"
>
```

修改为：

```tsx
<button
  className="nav-link nav-more-trigger locale-switch"
  onClick={() => setLocaleOpen(!localeOpen)}
  aria-expanded={localeOpen}
  aria-haspopup="true"
  aria-label={t("switchToLocale", { locale: localeLabels[currentLocale] })}
>
```

注意：`t` 来自已有的 `useTranslations("nav")`，`switchToLocale` key 已在所有 7 个 locale 消息文件中存在。

**Step 2: tsc + build + commit**

```bash
pnpm --filter @dtax/web exec tsc --noEmit
pnpm --filter @dtax/web build
git commit -m "fix(web): add aria-label to locale switcher button for accessibility"
```

---

### Task 2: form8949-pdf.ts 大文件重构

**Files:**

- Create: `packages/tax-engine/src/reports/pdf/render-form8949.ts`
- Create: `packages/tax-engine/src/reports/pdf/render-schedule-d.ts`
- Create: `packages/tax-engine/src/reports/pdf/pdf-utils.ts`
- Modify: `packages/tax-engine/src/reports/form8949-pdf.ts` (变为 re-export hub)

**目标：** 将 451 行拆分为 3 个专注模块，每个 <200 行。公共 API 不变。

**Step 1: 创建 `pdf-utils.ts` — 共享常量和工具函数**

```typescript
/**
 * PDF generation shared constants and utilities.
 * @license AGPL-3.0
 */

export const MARGIN = 50;
export const PAGE_WIDTH = 612; // Letter
export const CONTENT_WIDTH = PAGE_WIDTH - 2 * MARGIN;
export const LINES_PER_PAGE = 14;

/** Format number as US currency (2 decimal places). */
export function fmt(n: number): string {
  return n.toLocaleString("en-US", {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  });
}

/** Box descriptions for IRS Form 8949. */
export const BOX_DESCRIPTIONS: Record<string, string> = {
  A: "Short-term — basis reported to IRS",
  B: "Short-term — basis NOT reported to IRS",
  C: "Short-term — not on Form 1099-B",
  D: "Long-term — basis reported to IRS",
  E: "Long-term — basis NOT reported to IRS",
  F: "Long-term — not on Form 1099-B",
};

/** Standard PDF footer line. */
export function renderFooter(doc: PDFKit.PDFDocument): void {
  doc
    .fontSize(7)
    .font("Helvetica")
    .fillColor("#888888")
    .text(
      "Generated by DTax (getdtax.com) — For informational purposes only",
      MARGIN,
      740,
      { align: "center", width: CONTENT_WIDTH },
    );
  doc.fillColor("#000000");
}
```

**Step 2: 创建 `render-form8949.ts` — Form 8949 页面渲染**

从原文件提取 `PageOptions` 接口和 `renderForm8949Page()` 函数（原第 101-276 行）。

```typescript
/**
 * IRS Form 8949 page renderer.
 * @license AGPL-3.0
 */

import type { Form8949Line, Form8949BoxSummary } from "../form8949";
import {
  MARGIN,
  CONTENT_WIDTH,
  BOX_DESCRIPTIONS,
  fmt,
  renderFooter,
} from "./pdf-utils";

export interface PageOptions {
  box: string;
  taxYear: number;
  lines: Form8949Line[];
  summary?: Form8949BoxSummary;
  taxpayerName?: string;
  taxpayerSSN?: string;
  pageNum: number;
  totalPages: number;
}

export function renderForm8949Page(
  doc: PDFKit.PDFDocument,
  opts: PageOptions,
): void {
  // ... 原第 113-275 行代码完整复制 ...
  // 将 boxDesc 替换为导入的 BOX_DESCRIPTIONS
  // 末尾 footer 替换为 renderFooter(doc)
}
```

**Step 3: 创建 `render-schedule-d.ts` — Schedule D 页面渲染**

从原文件提取 `renderScheduleDPage()` 函数（原第 278-451 行）。

```typescript
/**
 * IRS Schedule D page renderer.
 * @license AGPL-3.0
 */

import type { ScheduleDReport } from "../schedule-d";
import { MARGIN, CONTENT_WIDTH, fmt, renderFooter } from "./pdf-utils";

export function renderScheduleDPage(
  doc: PDFKit.PDFDocument,
  scheduleD: ScheduleDReport,
  taxpayerName?: string,
): void {
  // ... 原第 282-451 行代码完整复制 ...
  // 末尾 footer 替换为 renderFooter(doc)
}
```

**Step 4: 将 `form8949-pdf.ts` 改为 re-export hub**

原 451 行文件替换为 ~50 行：

```typescript
/**
 * IRS Form 8949 PDF Generator
 *
 * Generates a structured PDF report matching the layout of IRS Form 8949.
 * Each page contains up to 14 line items, with Box designation and summary totals.
 *
 * @license AGPL-3.0
 */

import PDFDocument from "pdfkit";
import type { Form8949Report, Form8949Line } from "./form8949";
import type { ScheduleDReport } from "./schedule-d";
import { LINES_PER_PAGE } from "./pdf/pdf-utils";
import { renderForm8949Page } from "./pdf/render-form8949";
import { renderScheduleDPage } from "./pdf/render-schedule-d";

/**
 * Generate Form 8949 PDF as a Buffer.
 */
export function generateForm8949Pdf(
  report: Form8949Report,
  options?: {
    taxpayerName?: string;
    taxpayerSSN?: string;
    scheduleD?: ScheduleDReport;
  },
): Promise<Buffer> {
  return new Promise((resolve, reject) => {
    const doc = new PDFDocument({ size: "letter", margin: 50 });
    const chunks: Buffer[] = [];

    doc.on("data", (chunk: Buffer) => chunks.push(chunk));
    doc.on("end", () => resolve(Buffer.concat(chunks)));
    doc.on("error", reject);

    const linesByBox = new Map<string, Form8949Line[]>();
    for (const line of report.lines) {
      const arr = linesByBox.get(line.box) || [];
      arr.push(line);
      linesByBox.set(line.box, arr);
    }

    let isFirstPage = true;
    for (const [box, lines] of linesByBox) {
      const summary = report.boxSummaries.find((s) => s.box === box);
      const totalPages = Math.ceil(lines.length / LINES_PER_PAGE);

      for (let page = 0; page < totalPages; page++) {
        if (!isFirstPage) doc.addPage();
        isFirstPage = false;

        renderForm8949Page(doc, {
          box,
          taxYear: report.taxYear,
          lines: lines.slice(
            page * LINES_PER_PAGE,
            (page + 1) * LINES_PER_PAGE,
          ),
          summary: page === totalPages - 1 ? summary : undefined,
          taxpayerName: options?.taxpayerName,
          taxpayerSSN: options?.taxpayerSSN,
          pageNum: page + 1,
          totalPages,
        });
      }
    }

    if (options?.scheduleD) {
      doc.addPage();
      renderScheduleDPage(doc, options.scheduleD, options.taxpayerName);
    }

    doc.end();
  });
}
```

**Step 5: tsc + tests + commit**

```bash
pnpm --filter @dtax/tax-engine exec tsc --noEmit
pnpm --filter @dtax/tax-engine test -- --run
pnpm -r exec tsc --noEmit
git commit -m "refactor(tax-engine): split form8949-pdf.ts into 3 focused modules"
```

---

### Task 3: CLI 集成测试扩展

**Files:**

- Modify: `packages/cli/src/__tests__/cli-integration.test.ts`

**目标：** 从 13 个集成测试扩展到 30+，覆盖更多边界情况和高级功能。

**Step 1: 添加高级功能测试**

在现有 `cli-integration.test.ts` 中添加以下 describe 块：

```typescript
describe("Specific ID Method", () => {
  it("should calculate with Specific ID method", () => {
    const output = runCli(`calculate ${COINBASE} --method SPECIFIC_ID`);
    expect(output).toContain("Specific");
  });
});

describe("International Methods", () => {
  it("should calculate with Germany FIFO method", () => {
    const output = runCli(`calculate ${COINBASE} --method GERMANY_FIFO`);
    expect(output).toContain("GERMANY_FIFO");
  });

  it("should calculate with PMPA (French) method", () => {
    const output = runCli(`calculate ${COINBASE} --method PMPA`);
    expect(output).toContain("PMPA");
  });

  it("should calculate with Total Average (Japanese) method", () => {
    const output = runCli(`calculate ${COINBASE} --method TOTAL_AVERAGE`);
    expect(output).toContain("TOTAL_AVERAGE");
  });
});

describe("Currency Conversion", () => {
  it("should convert output to EUR", () => {
    const output = runCli(`calculate ${COINBASE} --currency EUR`);
    expect(output).toBeDefined();
  });

  it("should convert output to JPY", () => {
    const output = runCli(`calculate ${COINBASE} --currency JPY`);
    expect(output).toBeDefined();
  });
});

describe("JSON Output Validation", () => {
  it("should include method in JSON output", () => {
    const output = runCli(`calculate ${COINBASE} --json --method LIFO`);
    const jsonStart = output.indexOf("{");
    const parsed = JSON.parse(output.slice(jsonStart));
    expect(parsed.method).toBe("LIFO");
  });

  it("should include results array in JSON", () => {
    const output = runCli(`calculate ${COINBASE} --json`);
    const jsonStart = output.indexOf("{");
    const parsed = JSON.parse(output.slice(jsonStart));
    expect(Array.isArray(parsed.results)).toBe(true);
  });

  it("should include netGainLoss as number", () => {
    const output = runCli(`calculate ${COINBASE} --json`);
    const jsonStart = output.indexOf("{");
    const parsed = JSON.parse(output.slice(jsonStart));
    expect(typeof parsed.netGainLoss).toBe("number");
  });
});

describe("Compare Output", () => {
  it("should include all 7 methods in comparison", () => {
    const output = runCli(`calculate ${COINBASE} --compare`);
    expect(output).toContain("FIFO");
    expect(output).toContain("LIFO");
    expect(output).toContain("HIFO");
  });

  it("should show net gain/loss for each method", () => {
    const output = runCli(`calculate ${COINBASE} --compare`);
    // Each method row should have a dollar amount
    const lines = output.split("\n").filter((l) => l.includes("FIFO"));
    expect(lines.length).toBeGreaterThan(0);
  });
});

describe("Edge Cases", () => {
  it("should handle empty CSV gracefully", () => {
    const emptyFile = `/tmp/dtax-empty-${Date.now()}.csv`;
    fs.writeFileSync(emptyFile, "date,type,asset,amount,price\n");
    try {
      const output = runCli(`calculate ${emptyFile}`);
      expect(output).toBeDefined();
    } catch {
      // May throw on empty — that's acceptable
    }
    fs.unlinkSync(emptyFile);
  });

  it("should error on invalid method", () => {
    expect(() => {
      runCli(`calculate ${COINBASE} --method INVALID`);
    }).toThrow();
  });

  it("should error on missing file argument", () => {
    expect(() => {
      runCli("calculate");
    }).toThrow();
  });
});

describe("Output Format Options", () => {
  it("should write CSV output file", () => {
    const tmpFile = `/tmp/dtax-csv-${Date.now()}.csv`;
    runCli(`calculate ${COINBASE} --output ${tmpFile}`);
    const content = fs.readFileSync(tmpFile, "utf-8");
    expect(content).toContain(",");
    fs.unlinkSync(tmpFile);
  });

  it("should support --year filter with output", () => {
    const tmpFile = `/tmp/dtax-year-${Date.now()}.csv`;
    runCli(`calculate ${COINBASE} --year 2024 --output ${tmpFile}`);
    expect(fs.existsSync(tmpFile)).toBe(true);
    fs.unlinkSync(tmpFile);
  });
});

describe("Multiple Input Files", () => {
  it("should merge coinbase and binance files", () => {
    const binance = path.join(FIXTURE_DIR, "binance-trades.csv");
    const output = runCli(`calculate ${COINBASE} ${binance} --json`);
    const jsonStart = output.indexOf("{");
    const parsed = JSON.parse(output.slice(jsonStart));
    expect(parsed.results.length).toBeGreaterThan(0);
  });

  it("should compare methods across merged files", () => {
    const binance = path.join(FIXTURE_DIR, "binance-trades.csv");
    const output = runCli(`calculate ${COINBASE} ${binance} --compare`);
    expect(output).toContain("FIFO");
  });
});
```

注意：需要先 `pnpm --filter @dtax/cli build` 确保 CLI dist 是最新的。

**Step 2: tsc + build + tests + commit**

```bash
pnpm --filter @dtax/cli build
pnpm --filter @dtax/cli exec tsc --noEmit
pnpm --filter @dtax/cli test -- --run
git commit -m "test(cli): expand integration tests from 13 to 30+ cases"
```

---

## Execution Notes

- 三个 Task 完全独立，无依赖
- 每个 Task 结束后执行五步法审计：tsc → tests → build → commit → push
- Task 1 最简单（1 行代码改动），Task 2 最复杂（文件拆分），Task 3 纯测试
- form8949-pdf 重构不改变公共 API，所有测试应原样通过
- CLI 测试需要先 build CLI 到 dist/，因为集成测试运行编译后的 JS
