# npm 包正式发布 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 将 `@dtax/tax-engine`、`@dtax/shared-types`、`@dtax/cli` 正式发布到 npm registry，建立开发者社区入口。

**Architecture:** 使用 changesets 管理版本，GitHub Actions 自动发布。tax-engine 和 shared-types 为 AGPL-3.0，cli 为 AGPL-3.0。发布前确保 README、CHANGELOG、LICENSE 完整。

**Tech Stack:** npm, changesets, GitHub Actions, tsup

---

### Task 1: 预发布审计 — package.json 完善

**Files:**

- Modify: `packages/tax-engine/package.json`
- Modify: `packages/shared-types/package.json`
- Modify: `packages/cli/package.json`

**Step 1: 审查并完善每个 package.json**

确保每个包包含:

```json
{
  "name": "@dtax/tax-engine",
  "version": "0.1.0",
  "description": "Open-source crypto tax calculation engine — FIFO, LIFO, HIFO, Specific ID, wash sales, Form 8949",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/dTaxLab/dtax",
    "directory": "packages/tax-engine"
  },
  "homepage": "https://getdtax.com",
  "bugs": "https://github.com/dTaxLab/dtax/issues",
  "keywords": [
    "crypto",
    "tax",
    "bitcoin",
    "fifo",
    "cost-basis",
    "form-8949",
    "capital-gains",
    "wash-sale"
  ],
  "engines": { "node": ">=20.0.0" },
  "files": ["dist", "LICENSE", "README.md"],
  "publishConfig": { "access": "public" }
}
```

**Step 2: 验证 exports 配置正确**

确保 `exports`, `main`, `module`, `types` 字段指向 `dist/` 下正确文件。

**Step 3: Commit**

```bash
git add packages/*/package.json
git commit -m "chore: prepare package.json files for npm publishing"
```

---

### Task 2: README 完善 — 每个包独立 README

**Files:**

- Modify: `packages/tax-engine/README.md`
- Modify: `packages/cli/README.md`
- Modify: `packages/shared-types/README.md`

**Step 1: tax-engine README**

包含：

- 安装命令 `npm install @dtax/tax-engine`
- 快速开始示例（FIFO 计算、CSV 解析、Form 8949 生成）
- 支持的交易所列表（23 个）
- 支持的成本基础方法（4 种）
- API 文档链接
- License 说明

**Step 2: cli README**

包含：

- 安装命令 `npm install -g @dtax/cli`
- 使用示例（基本计算、多方法对比、导出 Form 8949）
- 完整 flag 列表
- License 说明

**Step 3: shared-types README**

包含：

- 安装命令
- 类型列表
- 使用示例

**Step 4: Commit**

```bash
git add packages/*/README.md
git commit -m "docs: add comprehensive README for npm packages"
```

---

### Task 3: 安装 Changesets 版本管理

**Files:**

- Create: `.changeset/config.json`
- Modify: `package.json` (root)

**Step 1: 安装 changesets**

Run: `pnpm add -D -w @changesets/cli @changesets/changelog-github`

**Step 2: 初始化 changesets**

Run: `pnpm changeset init`

**Step 3: 配置 .changeset/config.json**

```json
{
  "$schema": "https://unpkg.com/@changesets/config@3.0.0/schema.json",
  "changelog": ["@changesets/changelog-github", { "repo": "dTaxLab/dtax" }],
  "commit": false,
  "fixed": [],
  "linked": [["@dtax/tax-engine", "@dtax/shared-types"]],
  "access": "public",
  "baseBranch": "main",
  "updateInternalDependencies": "patch",
  "ignore": ["@dtax/api", "@dtax/web"]
}
```

**Step 4: 添加 root scripts**

```json
{
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version",
    "publish-packages": "pnpm build && changeset publish"
  }
}
```

**Step 5: Commit**

```bash
git add .changeset/ package.json pnpm-lock.yaml
git commit -m "chore: setup changesets for npm version management"
```

---

### Task 4: GitHub Actions — 自动发布 Workflow

**Files:**

- Create: `.github/workflows/publish.yml`

**Step 1: 创建发布 workflow**

```yaml
name: Publish Packages

on:
  push:
    branches: [main]

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          registry-url: "https://registry.npmjs.org"

      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      - name: Create Release PR or Publish
        uses: changesets/action@v1
        with:
          publish: pnpm publish-packages
          title: "chore: release packages"
          commit: "chore: release packages"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
```

**Step 2: 验证 workflow 语法**

Run: `cd .github/workflows && cat publish.yml | python3 -c "import sys,yaml; yaml.safe_load(sys.stdin)"`
Expected: 无错误

**Step 3: Commit**

```bash
git add .github/workflows/publish.yml
git commit -m "ci: add GitHub Actions workflow for npm package publishing"
```

---

### Task 5: 构建验证 & Dry Run

**Step 1: 构建所有包**

Run: `pnpm build`
Expected: 所有包构建成功

**Step 2: 验证包内容**

Run: `cd packages/tax-engine && npm pack --dry-run`
Expected: 显示将发布的文件列表，不含 src/ 或 test 文件

Run: `cd packages/cli && npm pack --dry-run`
Run: `cd packages/shared-types && npm pack --dry-run`

**Step 3: 本地测试安装**

Run: `cd /tmp && mkdir test-dtax && cd test-dtax && npm init -y && npm install /Users/ericw/project/dtax/packages/tax-engine`
Expected: 安装成功，`require('@dtax/tax-engine')` 可用

**Step 4: Commit (if any fixes needed)**

```bash
git commit -m "fix: package build and distribution fixes for npm publish"
```

---

### Task 6: 五步法审计 & 首次发布

**Step 1: 类型安全审计**

Run: `pnpm -r exec tsc --noEmit`
Expected: 0 errors

**Step 2: 测试回归审计**

Run: `pnpm test`
Expected: 全部通过

**Step 3: 安全合规审计**

- 确认 `.npmignore` 或 `files` 字段排除了 `.env`、`tests`、`prisma/` 等
- 确认不含任何 secret 或 API key

**Step 4: i18n 完整性审计**

N/A（纯包发布）

**Step 5: 构建部署审计**

Run: `pnpm build && pnpm publish-packages --dry-run`
Expected: dry-run 显示将发布 3 个包

**首次发布:**

1. 在 npm 网站创建 `@dtax` org
2. 设置 GitHub Secret: `NPM_TOKEN`
3. 创建首个 changeset: `pnpm changeset` → 选择 minor → 描述 "Initial public release"
4. Run: `pnpm version-packages && pnpm publish-packages`
