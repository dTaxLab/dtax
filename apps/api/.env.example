# DTax API Environment Variables
# Copy to .env and fill in values

DATABASE_URL="postgresql://dtax:dtax_dev_password@localhost:5433/dtax?schema=public"
HOST="0.0.0.0"
PORT="3001"
NODE_ENV="development"

# CORS
CORS_ORIGIN="http://localhost:3000"

# JWT (future)
# JWT_SECRET="your-secret-here"
